# Аналитический дашборд для TGWhisper Bot

## Обзор

Система аналитики для отслеживания продуктовых метрик Telegram-бота, включающая:

- **DAU/MAU** - Daily/Monthly Active Users (дневная/месячная активная аудитория)
- **Конверсия** - процент пользователей, успешно обработавших голосовые сообщения
- **Retention** - удержание пользователей (возврат через 7 дней)
- **Популярность профилей** - статистика использования профилей форматирования
- **Производительность** - метрики скорости обработки и узкие места
- **Error rate** - процент успешных операций и типы ошибок
- **Паттерны использования** - активность по часам и дням недели

## Быстрый старт

### 1. Настройка администраторского доступа

Добавьте ваш Telegram user ID в `.env`:

```bash
ADMIN_USER_IDS=304287083
```

Для нескольких администраторов используйте запятую:

```bash
ADMIN_USER_IDS=304287083,123456789
```

**Как узнать свой User ID:**
- Напишите боту [@userinfobot](https://t.me/userinfobot) в Telegram
- Или проверьте базу данных: `sqlite3 bot_database.db "SELECT id, username FROM users"`

### 2. Использование

После запуска бота команда `/analytics` будет доступна только администраторам:

1. Отправьте боту `/analytics`
2. Выберите период анализа:
   - 📊 7 дней
   - 📊 30 дней
   - 📊 90 дней
   - 📊 Всё время
3. Бот сгенерирует и отправит два файла:
   - **ZIP-архив с CSV** файлами (для Excel/Google Sheets)
   - **JSON** файл (для программного анализа)

### 3. Краткая сводка

При вызове `/analytics` бот сразу показывает краткую сводку:

```
📊 Аналитика бота

Краткая сводка (30 дней):
👥 DAU (сегодня): 15
👥 MAU: 42
📝 Всего пользователей: 50
✅ Success rate: 97.7%
📨 Всего запросов: 342
```

## Структура экспорта

### CSV (ZIP архив)

Архив содержит 5 файлов:

1. **summary.csv** - Основные метрики
   - DAU, MAU, Total Users
   - Conversion Rate, Retention Rate
   - Total Processed, Success Rate

2. **profile_stats.csv** - Популярность профилей
   - Название профиля
   - Количество использований
   - Процент от общего использования
   - Системный или пользовательский

3. **performance.csv** - Производительность
   - Среднее время обработки
   - Максимальное/минимальное время
   - Средняя длительность аудио
   - Efficiency ratio
   - Самые медленные профили
   - Количество выбросов

4. **errors.csv** - Ошибки
   - Общее количество запросов
   - Количество ошибок
   - Success rate / Error rate
   - Топ-5 частых ошибок

5. **usage_patterns.csv** - Паттерны использования
   - Почасовая статистика (0-23 часа)
   - По дням недели (Понедельник-Воскресенье)

### JSON формат

Единый JSON-файл со всеми метриками:

```json
{
  "export_date": "2026-01-06T16:30:00Z",
  "period_days": 30,
  "summary": {
    "dau_today": 15,
    "mau": 42,
    "total_users": 50,
    "conversion_rate": 68.5,
    "retention_rate_7d": 45.2
  },
  "profile_popularity": [...],
  "performance": {...},
  "bottlenecks": {...},
  "error_rates": {...},
  "usage_patterns": {
    "by_hour": {...},
    "by_weekday": {...}
  }
}
```

## Метрики в деталях

### DAU (Daily Active Users)
Количество уникальных пользователей, которые взаимодействовали с ботом сегодня.
Считается по последнему времени активности (`users.last_activity`).

### MAU (Monthly Active Users)
Количество уникальных пользователей за выбранный период (7/30/90 дней).

### Conversion Rate
**Формула:** (Пользователи с успешной обработкой / Всего пользователей) × 100%

Показывает, какой процент зарегистрированных пользователей фактически использовал основную функцию бота (обработку голосовых сообщений).

### Retention Rate (7 дней)
**Формула:** (Пользователи, вернувшиеся через 7+ дней / Пользователи в когорте) × 100%

Измеряет лояльность: какой процент новых пользователей продолжает использовать бота через неделю после регистрации.

### Profile Popularity
Статистика использования каждого профиля форматирования:
- Абсолютное количество использований
- Процент от общего числа
- Тип профиля (системный/пользовательский)

Помогает понять, какие стили форматирования наиболее востребованы.

### Performance Metrics
- **Average processing time** - среднее время обработки запроса
- **Efficiency ratio** - отношение времени обработки к длительности аудио (должно быть < 1.0 для хорошей производительности)
- **Outliers** - запросы, которые обрабатывались в 2+ раза дольше среднего

### Error Rates
- **Success rate** - процент успешно обработанных запросов
- **Common errors** - топ-5 самых частых ошибок с количеством

### Usage Patterns
- **By hour** - пиковые часы активности (помогает планировать обслуживание)
- **By weekday** - активность по дням недели

## Техническая архитектура

### Файлы

```
services/analytics_service.py    # Основной движок расчёта метрик
bot/handlers/analytics.py        # Telegram-обработчик команды
config.py                         # ADMIN_USER_IDS конфигурация
database/database.py              # Индексы для производительности
```

### Индексы базы данных

Для оптимальной производительности созданы следующие индексы:

```sql
CREATE INDEX idx_usage_logs_user_created ON usage_logs(user_id, created_at);
CREATE INDEX idx_usage_logs_profile_created ON usage_logs(profile_id, created_at);
CREATE INDEX idx_usage_logs_success_created ON usage_logs(success, created_at);
CREATE INDEX idx_users_last_activity ON users(last_activity);
CREATE INDEX idx_users_created_at ON users(created_at);
```

Эти индексы автоматически создаются при первом запуске бота после обновления.

### Источники данных

Вся аналитика основана на существующих таблицах:

- **users** - регистрации, последняя активность
- **usage_logs** - детальные логи всех обработок (время, успех/ошибка, профиль, длительность)
- **profiles** - информация о профилях форматирования

Новые таблицы не создаются - используется уже собранная информация.

## Безопасность

- ✅ Доступ только для администраторов (проверка user ID)
- ✅ Двойная проверка прав (в команде и callback)
- ✅ Логирование всех попыток доступа
- ✅ Конфиденциальность: пользователи не видят чужие данные

## Производительность

- Оптимизировано для Beget.com хостинга
- Все запросы используют индексы
- Генерация отчёта занимает < 5 секунд при 10,000+ записей
- Асинхронная обработка (не блокирует бота)

## Часто задаваемые вопросы

**Q: Можно ли дать доступ нескольким администраторам?**  
A: Да, просто перечислите их user ID через запятую в `ADMIN_USER_IDS`.

**Q: Как сбросить статистику?**  
A: Удалите записи из таблицы `usage_logs` в SQLite базе данных.

**Q: Можно ли добавить свои метрики?**  
A: Да, расширьте класс `AnalyticsService` в `services/analytics_service.py`.

**Q: Данные экспортируются в реальном времени?**  
A: Да, каждый экспорт генерируется на основе текущего состояния базы данных.

**Q: Влияет ли аналитика на производительность бота?**  
A: Нет, расчёты выполняются только по запросу администратора, никакого фонового сбора данных нет.

## Будущие улучшения

Планируемые фичи для следующих версий:

- [ ] Автоматическая еженедельная рассылка дайджеста
- [ ] Веб-дашборд с графиками (Grafana/Plotly)
- [ ] A/B тестирование промптов профилей
- [ ] Алерты при падении success rate
- [ ] Детальный когортный анализ
- [ ] Экспорт в Google Sheets API

## Откат изменений

Если нужно отменить добавление аналитики:

```bash
git checkout main
git branch -D feature/analytics-dashboard
```

Бот продолжит работать как обычно, индексы останутся (они не мешают).

---

**Разработано с помощью [Claude Code](https://claude.com/claude-code)**
