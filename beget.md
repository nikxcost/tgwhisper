# Инструкция по развертыванию бота на Beget.com

Полное руководство по размещению Telegram бота tgwhisper на виртуальном хостинге Beget.com.

## Содержание

1. [Подготовка к развертыванию](#1-подготовка-к-развертыванию)
2. [Подключение к Beget по SSH](#2-подключение-к-beget-по-ssh)
3. [Первоначальная настройка](#3-первоначальная-настройка)
4. [Настройка переменных окружения](#4-настройка-переменных-окружения)
5. [Первый запуск и тестирование](#5-первый-запуск-и-тестирование)
6. [Настройка автозапуска через cron](#6-настройка-автозапуска-через-cron)
7. [Управление ботом](#7-управление-ботом)
8. [Обновление бота](#8-обновление-бота)
9. [Мониторинг и логи](#9-мониторинг-и-логи)
10. [Резервное копирование](#10-резервное-копирование)
11. [Решение проблем](#11-решение-проблем)

---

## 1. Подготовка к развертыванию

### Что вам понадобится

✅ **Аккаунт на Beget.com** с активным виртуальным хостингом
✅ **Доступ по SSH** (включите в панели управления Beget)
✅ **Telegram Bot Token** - получите у [@BotFather](https://t.me/BotFather)
✅ **OpenRouter API Key** - зарегистрируйтесь на [openrouter.ai](https://openrouter.ai/)
✅ **Groq API Key** - зарегистрируйтесь на [console.groq.com](https://console.groq.com/)

### Проверка репозитория

Убедитесь, что код залит на GitHub:

```bash
# Локально на вашем компьютере
cd /Users/vladimir/tgwhisper
git status
git remote -v
```

Если remote не настроен, добавьте:

```bash
git remote add origin https://github.com/nikxcost/tgwhisper.git
git push -u origin main
```

---

## 2. Подключение к Beget по SSH

### Получение данных для подключения

1. Войдите в [панель управления Beget](https://cp.beget.com/)
2. Перейдите в раздел **"SSH/Терминал"** или **"Доступы"**
3. Найдите данные для подключения:
   - Хост: `имя_хоста.beget.tech`
   - Логин: ваш логин (обычно совпадает с именем аккаунта)
   - Порт: обычно 22

### Подключение

Откройте терминал и выполните:

```bash
ssh ваш_логин@имя_хоста.beget.tech
```

Пример:

```bash
ssh u123456@u123456.beget.tech
```

При первом подключении подтвердите fingerprint (введите `yes`).

### Проверка окружения

После успешного подключения проверьте доступную версию Python:

```bash
python3 --version
```

Должна быть версия Python 3.8 или выше. Если версия старая, проверьте другие варианты:

```bash
ls /usr/bin/python*
python3.10 --version  # попробуйте разные версии
```

---

## 3. Первоначальная настройка

### Клонирование репозитория

На сервере Beget выполните:

```bash
# Перейдите в домашнюю директорию
cd ~

# Клонируйте проект
git clone https://github.com/nikxcost/tgwhisper.git

# Перейдите в директорию проекта
cd tgwhisper

# Проверьте содержимое
ls -la
```

### Запуск скрипта установки

Мы создали автоматический скрипт установки, который:
- Создаст виртуальное окружение Python
- Установит все зависимости
- Создаст необходимые директории
- Подготовит шаблон .env файла

Запустите:

```bash
chmod +x deploy/setup.sh
./deploy/setup.sh
```

Скрипт выведет пошаговую информацию о процессе установки.

**Важно:** В конце установки скрипт покажет следующие шаги. Не закрывайте терминал!

---

## 4. Настройка переменных окружения

### Редактирование .env файла

После выполнения setup.sh у вас появится файл `.env`. Отредактируйте его:

```bash
nano .env
```

Заполните ваши реальные ключи:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz12345678

# API Keys
OPENROUTER_API_KEY=sk-or-v1-abcdef1234567890
GROQ_API_KEY=gsk_abcdef1234567890

# Database
DATABASE_URL=sqlite:///bot_database.db

# Logging
LOG_LEVEL=INFO
```

**Как сохранить в nano:**
1. Вставьте ключи
2. Нажмите `Ctrl + O` (сохранить)
3. Нажмите `Enter` (подтвердить имя файла)
4. Нажмите `Ctrl + X` (выйти)

### Где получить API ключи

**Telegram Bot Token:**
1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot` или используйте существующего бота
4. Скопируйте токен

**OpenRouter API Key:**
1. Зарегистрируйтесь на [openrouter.ai](https://openrouter.ai/)
2. Пополните баланс на $1-5 (используется бесплатная модель, но нужен депозит)
3. Перейдите в [Keys](https://openrouter.ai/keys)
4. Создайте новый ключ

**Groq API Key:**
1. Зарегистрируйтесь на [console.groq.com](https://console.groq.com/)
2. Перейдите в раздел API Keys
3. Создайте новый ключ
4. Скопируйте (бесплатный лимит: 14,400 запросов/день)

---

## 5. Первый запуск и тестирование

### Ручной запуск для проверки

Перед настройкой автозапуска проверим, что бот работает:

```bash
cd ~/tgwhisper

# Активируйте виртуальное окружение
source venv/bin/activate

# Запустите бота
python3 main.py
```

**Что вы должны увидеть:**

```
🔧 Инициализация базы данных...
✅ База данных готова
🚀 Бот запущен и готов к работе!
```

### Тестирование в Telegram

1. Откройте Telegram
2. Найдите вашего бота по имени
3. Отправьте команду `/start`
4. Бот должен ответить приветственным сообщением
5. Попробуйте отправить голосовое сообщение

**Если бот отвечает - всё работает!** ✅

### Остановка бота

Вернитесь в терминал и остановите бота:

```
Нажмите Ctrl + C
```

Бот должен корректно завершиться.

---

## 6. Настройка автозапуска через cron

Теперь настроим автоматический запуск бота 24/7 через cron.

### Как это работает

На виртуальном хостинге Beget нет systemd, поэтому мы используем **cron** (планировщик задач):
- Cron запускает скрипт `supervisor.sh` каждые 3 минуты
- Скрипт проверяет, работает ли бот
- Если бот упал или не запущен, скрипт автоматически запустит его

### Узнайте полный путь к проекту

```bash
cd ~/tgwhisper
pwd
```

Скопируйте вывод, например: `/home/u123456/tgwhisper`

### Настройка crontab

Откройте редактор cron:

```bash
crontab -e
```

При первом запуске может спросить редактор - выберите `nano` (обычно вариант 1).

Добавьте следующие строки в **конец файла**:

```cron
# Автозапуск и надзор за ботом (каждые 3 минуты)
*/3 * * * * /home/u123456/tgwhisper/deploy/supervisor.sh

# Резервное копирование (ежедневно в 3:00 ночи)
0 3 * * * /home/u123456/tgwhisper/deploy/backup.sh

# Ротация логов (ежедневно в 2:00 ночи)
0 2 * * * /home/u123456/tgwhisper/deploy/rotate_logs.sh

# Оптимизация БД (1 числа каждого месяца в 1:00)
0 1 1 * * /home/u123456/tgwhisper/deploy/optimize_db.sh

# Мониторинг ресурсов (каждый час)
0 * * * * /home/u123456/tgwhisper/deploy/monitor.sh
```

**⚠️ ВАЖНО:** Замените `/home/u123456/tgwhisper` на ваш реальный путь из команды `pwd`!

Сохраните файл:
- `Ctrl + O`, затем `Enter`
- `Ctrl + X`

### Проверка crontab

Убедитесь, что задачи добавлены:

```bash
crontab -l
```

Должны отобразиться добавленные вами строки.

### Первый запуск через supervisor

Запустите supervisor вручную, чтобы проверить:

```bash
cd ~/tgwhisper
./deploy/supervisor.sh
```

Подождите 5 секунд и проверьте статус:

```bash
./deploy/check_status.sh
```

Вы должны увидеть:

```
=== TGWhisper Bot Status ===

Status: ✓ RUNNING
PID: 12345
Uptime: 00:00:05
Memory: 45.23 MB
```

**Готово!** Теперь cron будет автоматически следить за ботом.

---

## 7. Управление ботом

### Проверка статуса

```bash
cd ~/tgwhisper
./deploy/check_status.sh
```

### Остановка бота

```bash
./deploy/stop.sh
```

**Внимание:** Cron перезапустит бота в течение 3 минут. Если хотите остановить навсегда, удалите задачу из crontab.

### Перезапуск бота

```bash
./deploy/restart.sh
```

### Просмотр логов в реальном времени

```bash
# Логи бота
tail -f ~/tgwhisper/logs/bot.log

# Логи supervisor (автозапуск)
tail -f ~/tgwhisper/logs/supervisor.log

# Выход из просмотра: Ctrl + C
```

### Последние 50 строк лога

```bash
tail -50 ~/tgwhisper/logs/bot.log
```

### Поиск ошибок в логах

```bash
grep -i "error" ~/tgwhisper/logs/bot.log | tail -20
```

---

## 8. Обновление бота

Когда вы внесете изменения в код локально и загрузите на GitHub, обновите бота на сервере.

### Автоматическое обновление (рекомендуется)

```bash
cd ~/tgwhisper
./deploy/update.sh
```

Скрипт автоматически:
1. ✅ Создаст резервную копию
2. ✅ Остановит бота
3. ✅ Загрузит новый код через `git pull`
4. ✅ Обновит зависимости Python
5. ✅ Обновит базу данных (если нужно)
6. ✅ Перезапустит бота

Проверьте результат:

```bash
./deploy/check_status.sh
tail -20 ~/tgwhisper/logs/update.log
```

### Ручное обновление

Если нужен больший контроль:

```bash
cd ~/tgwhisper

# Остановка
./deploy/stop.sh

# Backup
./deploy/backup.sh

# Обновление кода
git pull origin main

# Обновление зависимостей
source venv/bin/activate
pip install -r requirements.txt --upgrade

# Перезапуск
./deploy/supervisor.sh
```

### Откат к предыдущей версии

Если обновление вызвало проблемы:

```bash
cd ~/tgwhisper

# Остановить бота
./deploy/stop.sh

# Посмотреть доступные бэкапы
ls -lht backups/

# Восстановить из бэкапа (замените дату на нужную)
tar -xzf backups/backup_20260103_150000.tar.gz

# Или откатить через git
git log --oneline -5  # посмотреть последние коммиты
git reset --hard COMMIT_HASH  # откатиться на конкретный коммит

# Перезапустить
./deploy/supervisor.sh
```

---

## 9. Мониторинг и логи

### Расположение файлов логов

```
~/tgwhisper/logs/
├── bot.log          # Основные логи бота (действия, ошибки)
├── supervisor.log   # Логи автозапуска через cron
├── update.log       # Логи обновлений
└── monitor.log      # Мониторинг ресурсов (память, диск)
```

### Просмотр активности бота

```bash
# Последние 30 строк
tail -30 ~/tgwhisper/logs/bot.log

# В реальном времени
tail -f ~/tgwhisper/logs/bot.log
```

### Проверка автозапусков

```bash
# Когда supervisor запускал бота
cat ~/tgwhisper/logs/supervisor.log
```

### Мониторинг ресурсов

```bash
# Использование памяти и диска
tail -20 ~/tgwhisper/logs/monitor.log
```

### Размер базы данных

```bash
du -h ~/tgwhisper/bot_database.db
```

### Статистика обработки

Подключитесь к базе данных:

```bash
cd ~/tgwhisper
sqlite3 bot_database.db
```

Выполните запросы:

```sql
-- Общее количество обработанных голосовых
SELECT COUNT(*) FROM usage_logs;

-- Успешные и неуспешные
SELECT success, COUNT(*) FROM usage_logs GROUP BY success;

-- Последние 10 записей
SELECT * FROM usage_logs ORDER BY created_at DESC LIMIT 10;

-- Выход
.exit
```

### Очистка больших логов вручную

Если лог-файл разросся:

```bash
# Посмотреть размер
du -h ~/tgwhisper/logs/*.log

# Очистить (осторожно!)
> ~/tgwhisper/logs/bot.log

# Или запустить ротацию
./deploy/rotate_logs.sh
```

---

## 10. Резервное копирование

### Автоматические бэкапы

Cron создает бэкапы **ежедневно в 3:00** (если вы добавили задачу из раздела 6).

Бэкапы хранятся в:

```bash
~/tgwhisper/backups/
```

Хранится последние **7 бэкапов**, старые автоматически удаляются.

### Ручное создание бэкапа

```bash
cd ~/tgwhisper
./deploy/backup.sh
```

Бэкап включает:
- ✅ База данных (`bot_database.db`)
- ✅ Все логи
- ✅ Файл `.env` с ключами

### Просмотр бэкапов

```bash
ls -lht ~/tgwhisper/backups/
```

### Восстановление из бэкапа

```bash
cd ~/tgwhisper

# Остановить бота
./deploy/stop.sh

# Восстановить файлы (замените имя файла на ваш бэкап)
tar -xzf backups/backup_20260103_030000.tar.gz

# Перезапустить
./deploy/supervisor.sh
```

### Скачивание бэкапа на локальный компьютер

На **вашем компьютере** (не на сервере):

```bash
# Через SCP
scp u123456@u123456.beget.tech:~/tgwhisper/backups/backup_20260103_030000.tar.gz ~/Downloads/

# Или через SFTP клиент (FileZilla, Cyberduck, etc.)
```

---

## 11. Решение проблем

### Проблема: Бот не запускается

**Симптомы:** `check_status.sh` показывает "STOPPED"

**Решение:**

```bash
# 1. Проверьте логи на ошибки
tail -50 ~/tgwhisper/logs/bot.log

# 2. Попробуйте запустить вручную
cd ~/tgwhisper
source venv/bin/activate
python3 main.py

# 3. Проверьте .env файл
cat .env | grep -v "^#" | grep -v "^$"

# 4. Проверьте, что все ключи заполнены
nano .env
```

Частые причины:
- ❌ Неправильный TELEGRAM_BOT_TOKEN
- ❌ Отсутствует один из API ключей
- ❌ Проблемы с доступом к базе данных

### Проблема: Бот падает через несколько минут

**Решение:**

```bash
# Проверьте ошибки в логах
grep -i "error\|exception" ~/tgwhisper/logs/bot.log | tail -30

# Проверьте память
free -h

# Проверьте дисковое пространство
df -h

# Включите DEBUG логирование
nano .env
# Измените: LOG_LEVEL=DEBUG

./deploy/restart.sh
```

### Проблема: Cron не перезапускает бота

**Решение:**

```bash
# 1. Проверьте, что cron задачи добавлены
crontab -l

# 2. Проверьте права на скрипт
ls -la ~/tgwhisper/deploy/supervisor.sh
chmod +x ~/tgwhisper/deploy/supervisor.sh

# 3. Проверьте логи supervisor
cat ~/tgwhisper/logs/supervisor.log

# 4. Запустите supervisor вручную
./deploy/supervisor.sh

# 5. Проверьте путь в crontab (должен быть абсолютный)
pwd  # скопируйте путь и обновите в crontab
```

### Проблема: "Permission denied" при запуске скриптов

**Решение:**

```bash
# Сделайте все скрипты исполняемыми
chmod +x ~/tgwhisper/deploy/*.sh

# Проверьте
ls -la ~/tgwhisper/deploy/
```

### Проблема: Git pull выдает ошибку конфликта

**Решение:**

```bash
cd ~/tgwhisper

# Сохранить локальные изменения
git stash

# Обновить
git pull origin main

# Или сбросить к удаленной версии (ПОТЕРЯЕТЕ локальные изменения)
git fetch origin
git reset --hard origin/main
```

### Проблема: База данных заблокирована

**Симптомы:** Ошибка "database is locked"

**Решение:**

```bash
# 1. Остановите бота
./deploy/stop.sh

# 2. Убедитесь, что нет других процессов
ps aux | grep python3

# 3. Проверьте целостность БД
cd ~/tgwhisper
sqlite3 bot_database.db "PRAGMA integrity_check;"

# 4. Перезапустите
./deploy/supervisor.sh
```

### Проблема: Бот не отвечает на голосовые сообщения

**Решение:**

```bash
# 1. Проверьте GROQ_API_KEY
grep GROQ_API_KEY ~/tgwhisper/.env

# 2. Проверьте лимиты на Groq
# Зайдите на console.groq.com и проверьте квоты

# 3. Проверьте OPENROUTER_API_KEY и баланс
# Зайдите на openrouter.ai и проверьте Credits

# 4. Посмотрите ошибки в логах
grep -A5 "whisper\|groq" ~/tgwhisper/logs/bot.log
```

### Проблема: Недостаточно места на диске

**Решение:**

```bash
# Проверьте использование
du -sh ~/tgwhisper/*

# Очистите старые логи
./deploy/rotate_logs.sh

# Удалите старые бэкапы (оставьте последний)
cd ~/tgwhisper/backups
ls -lt
rm backup_old_date.tar.gz

# Оптимизируйте базу данных
./deploy/optimize_db.sh
```

### Проблема: Высокое использование памяти

**Решение:**

```bash
# Проверьте память бота
./deploy/check_status.sh

# Перезапустите бота
./deploy/restart.sh

# Если проблема повторяется, проверьте логи
grep -i "memory" ~/tgwhisper/logs/bot.log
```

### Получение помощи

Если проблема не решается:

1. **Соберите информацию:**
   ```bash
   ./deploy/check_status.sh > status.txt
   tail -100 ~/tgwhisper/logs/bot.log > bot_log.txt
   tail -50 ~/tgwhisper/logs/supervisor.log > supervisor_log.txt
   ```

2. **Скачайте логи:**
   ```bash
   # На вашем компьютере
   scp u123456@u123456.beget.tech:~/tgwhisper/*.txt ~/Downloads/
   ```

3. **Создайте issue на GitHub** с описанием проблемы и логами

---

## Итоговая структура проекта на сервере

```
~/tgwhisper/
├── main.py                      # Точка входа
├── config.py                    # Конфигурация
├── .env                         # СЕКРЕТНЫЕ КЛЮЧИ (не commit)
├── .bot.pid                     # PID текущего процесса
├── .supervisor.lock             # Блокировка supervisor
├── bot_database.db              # База данных SQLite
├── requirements.txt             # Зависимости Python
├── venv/                        # Виртуальное окружение
├── bot/                         # Модули бота
├── database/                    # Модели БД
├── services/                    # Внешние API
├── utils/                       # Утилиты
├── logs/                        # Логи
│   ├── bot.log
│   ├── supervisor.log
│   ├── update.log
│   └── monitor.log
├── backups/                     # Резервные копии
│   └── backup_*.tar.gz
└── deploy/                      # Скрипты развертывания
    ├── supervisor.sh            # ← Запускается cron каждые 3 мин
    ├── setup.sh
    ├── stop.sh
    ├── restart.sh
    ├── check_status.sh
    ├── update.sh
    ├── backup.sh
    ├── rotate_logs.sh
    ├── optimize_db.sh
    └── monitor.sh
```

---

## Быстрая справка по командам

```bash
# Статус бота
./deploy/check_status.sh

# Остановить
./deploy/stop.sh

# Перезапустить
./deploy/restart.sh

# Обновить код
./deploy/update.sh

# Создать бэкап
./deploy/backup.sh

# Просмотр логов
tail -f logs/bot.log

# Проверка cron
crontab -l

# Редактирование .env
nano .env
```

---

## Поздравляем! 🎉

Ваш Telegram бот теперь работает **24/7** на Beget.com!

Что происходит автоматически:
- ✅ Бот перезапускается при сбоях (каждые 3 минуты проверка)
- ✅ Ежедневные резервные копии (3:00 ночи)
- ✅ Автоматическая ротация логов (2:00 ночи)
- ✅ Ежемесячная оптимизация БД (1 числа в 1:00)
- ✅ Почасовой мониторинг ресурсов

Наслаждайтесь использованием! 🚀
